// imports

// actions

// action creators

// api actions

// initial State

const initialState = {
  isLoggedIn: false
};

// reducer

function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

// reducer actions

// exports

// default reducer export

export default reducer;
